spring:
  h2:
    console:
      enabled: true
  datasource:
    url: "jdbc:h2:file:~/resources/database/moviesAwards"
    driverClassName: org.h2.Driver
    username: moviesAwards
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update

dataLoader:
  category.bestPicture: "Best Picture"
  url.omdbUrl: "http://www.omdbapi.com"
  url.apiKey: "51fb52b"

token:
  secret: "XxpRpfHX"